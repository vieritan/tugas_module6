[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["vieri <vieri@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0.3"
python-dotenv = "^1.0.1"
supabase = "^2.9.0"
coverage = "^7.6.2"
pytest-mock = "^3.14.0"


[tool.poetry.group.test.dependencies]
pytest = "^8.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
